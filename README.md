# ジョーブラ (JOE BROS) — MVP

ブラウザで動く、オリジナル3D対戦アクションゲームのMVP（プレイ可能な最小構成版）です。
「どこかで見たことがあるようなキャラクターたちが、破壊可能な3Dステージで吹き飛ばし合う」
ことをコンセプトに、衣装・名称・体型・技・設定はすべて独自のオリジナルとしています。

画像・3Dモデルの外部素材は一切使用していません。キャラクターはThree.jsの基本図形
（Box / Sphere / Cylinder / Cone / Capsule）を組み合わせた自作モデル、効果音はすべて
Web Audio APIによる生成音です。BGMのみ、AI生成による自作楽曲（`public/bgm/`）を使用
しています。

キャラクターモデルは人体比率にもとづく骨格の上に組み立てており、肩と股関節を支点に
動き、肘と膝で曲がる二節構造の四肢を持ちます。表情（目・眉・鼻・口）、衣服の縫い目や
装備といったディテールもすべて基本図形で作っています。

## 起動方法

```bash
npm install
npm run dev       # 開発サーバー起動（http://localhost:5173）
npm run build     # 本番ビルド（dist/ に出力）
npm run preview   # ビルド結果のプレビュー
npm run typecheck # 型チェックのみ実行
```

外部サーバーやバックエンドは不要です。`npm run build` 後の `dist/` フォルダは
そのまま静的ホスティング（GitHub Pages等）に配置して動作します。

## 公開（デプロイ）方法

> **重要:** リポジトリ直下をそのまま静的配信しても動きません（画面が白くなります）。
> `index.html` が読み込む `src/main.ts` は **TypeScript** のため、ブラウザは実行できず、
> `.ts` は JavaScript ではない MIME type で配信されてモジュール読み込みが拒否されます。
> 必ず `npm run build` で生成した `dist/` を配信してください。

### GitHub Pages

`.github/workflows/deploy.yml` により、`main` へのプッシュで自動的にビルドと公開が行われます。
初回のみ、リポジトリ側で以下の設定が必要です。

1. GitHub の **Settings → Pages** を開く
2. **Build and deployment → Source** を **「GitHub Actions」** に変更する
   （「Deploy from a branch」のままだと、ビルドされていない生のソースが配信されて白画面になります）

設定後、`main` にプッシュするか **Actions → Deploy to GitHub Pages → Run workflow** を
実行すると公開されます。公開URLは Actions の実行結果に表示されます。

### その他の静的ホスティング

```bash
npm ci
npm run build     # dist/ が生成される
```

生成された `dist/` の中身をそのまま配信してください。`vite.config.ts` の `base: "./"` に
より資産パスは相対参照になっているため、サブディレクトリ配下でもそのまま動作します。

## キャラクター選択

ゲーム起動時にキャラクター選択画面が表示されます。4人のファイターから1人を選ぶと、
残りの3人がCPUとして参戦します。

- 画面の空いた側に、選択中のファイターの3Dモデルが回転しながら表示されます
- ロースター内で比較した性能バー（スピード / パワー / ふっとばし / ジャンプ /
  防御 / 重さ / 必殺技）と必殺技名・クールダウンをその場で確認できます
- 操作: `←` `→` または `A` `D` でカーソル移動、`Enter` で決定。
  タッチ・マウスではカードをタップで選択し、もう一度タップで決定
- 選んだキャラクターは保存され、次回起動時にカーソルがそのキャラクターから始まります
- ポーズ画面・リザルト画面の「キャラクター選択」からいつでも選び直せます
  （決定すると新しい組み合わせで試合が最初から始まります）

## 操作方法

### PC

| キー | 動作 |
| --- | --- |
| W / A / S / D | 移動 |
| マウス移動 | カメラ視点操作（キャンバスをクリックしてポインターロック） |
| 左クリック | 通常攻撃（空中では空中攻撃 / アイテム所持中はアイテムを使用） |
| 右クリック | 強攻撃（**空中では急降下スパイク**） |
| E | 必殺技 |
| Q（長押し） | ガード |
| Shift（長押し） | ダッシュ |
| Ctrl または C | 回避（無敵時間あり / **崖つかまり中は手を離す**） |
| Space | ジャンプ（空中で1回だけ二段ジャンプ可 / **崖つかまり中はよじ登る**） |
| R | カメラ位置リセット |
| Esc | ポーズ |
| F3 | デバッグ表示切替 |

操作キーは `src/config/controls.ts` にまとめてあり、変更が容易な構造になっています。

### スマートフォン

- 画面左側: 仮想スティックで移動（スティックを大きく倒すとダッシュ）
- 画面右側: 攻撃 / 強攻撃 / 必殺技 / ジャンプ / ガード / 回避 の6ボタン
  （アイテム所持中は「攻撃」ボタンがアイテム使用に変わります）
- マルチタッチ対応（移動しながら攻撃・ジャンプ可能）
- 画面スクロールや長押しメニューが出ないように制御済み
- 画面サイズに応じてUIレイアウトが自動調整されます

タッチデバイス（`pointer: coarse` かつ画面幅900px未満）を自動検出してモバイル操作
UIを表示します。カメラは移動方向へ自動で追従します（マウス視点操作の代わり）。

## ゲームルール

- プレイヤー1人 vs CPU3人の3Dアリーナ対戦
- 攻撃を受けるとHPではなく**ダメージ%**が蓄積し、%が高いほど大きく吹き飛ぶ
- ステージ外へ落下、または一定の高さより下に落ちると1ストックを失う
- 初期ストックは3。最後までストックが残っていたキャラクターの勝利
- 制限時間（3分）到達時は、ストック数→ダメージ%の少なさで勝敗を判定
- 空中では二段ジャンプ・空中回避が1回ずつ使用可能（復帰用）
- ガードには耐久値があり、削り切られるとガードブレイクして一定時間行動不能に
- 回避には短い無敵時間とクールダウンがある
- 試合中はステージ上空からアイテムが落下する（下記「アイテム」参照）
- プレイヤーが全ストックを失った時点で試合終了（敗北）

### 空中攻撃とスパイク

地上と空中で技が変化します。空中の通常攻撃は地上より発生が速く威力は控えめ、
空中の強攻撃は各キャラクター固有の**スパイク**（相手を真下に叩き落とす技）です。

スパイクは発生が遅い代わりに威力が高く、**自分と同じ高さか下にいる相手にしか
当たりません**。場外に出た相手を復帰させないための技であり、外すと大きな隙を
さらします。

### 崖つかまり

落下中にステージの縁に近づくと、自動的に崖につかまります。

- つかまると**二段ジャンプ・空中回避が復活**し、短い無敵時間が発生します
- `Space` でよじ登り、`Ctrl`/`C`（回避）で手を離します
- 一定時間で自動的に手を離すため、崖に居座り続けることはできません
- **ふっとばされている最中（ヒットストップ中）はつかまれません** — まず二段
  ジャンプや回避で吹き飛びから復帰する必要があり、これがスパイクの狙い目です

### 吹き飛ばし計算

```
吹き飛ばし力 = (攻撃の基礎吹き飛ばし力 + 吹き飛ばし成長率 × 相手のダメージ%)
              × 攻撃側の吹き飛ばし力補正 ÷ 相手の重量
```

この式と各種係数は `src/config/gameConfig.ts`（全体バランス）と
`src/characters/attacks.ts`（攻撃ごとの数値）に分離してあり、コードを変更せず
数値調整だけでバランス調整ができます。

## キャラクター

| キャラクター | タイプ | 特徴 |
| --- | --- | --- |
| **ジョリオ** | 万能型 | 赤い作業着の下水道の王。扱いやすく近中距離両対応。必殺技「マンホールギザー」で前方の敵を打ち上げる |
| **ビリネズ** | 高速型 | コンセントに住む黄色い電気生物。速いが軽く吹き飛びやすい。必殺技「ジグザグスパーク」で前方をしびれさせる |
| **ハヤスギ** | 突進型 | 高速移動用スーツの青い戦士。最高速は随一だが曲がりにくい上級者向け。必殺技「ハイパーダッシュモード」で一定時間移動・攻撃力が上昇（操作は不安定に） |
| **ダンボール・ジョー** | 罠・遠距離型 | 段ボール装甲の潜入兵士。接近戦は弱いがロケット花火と地雷トラップで戦う（同時設置数に上限あり） |

プレイヤーが選ばなかった3キャラクターはCPUが担当します。CPUの性格はキャラクター
ごとに固定で（ジョリオ=攻撃的 / ビリネズ=攻撃的 / ハヤスギ=慎重 /
ダンボール・ジョー=遠距離重視）、`src/ai/AIState.ts` の `CHARACTER_PERSONALITY`
で定義しています。

キャラクターの数値（重量・移動速度・ジャンプ力・攻撃力・吹き飛ばし力・防御力・
必殺技性能）は `src/characters/characterData.ts` にデータとして分離されており、
新しいキャラクターを追加しやすい構造になっています。

## アイテム

試合中、ステージ上空から一定間隔でアイテムが落下します。**触れるだけで拾える**ので
専用の操作ボタンはありません（CPUもアイテムを狙って動きます）。

| アイテム | 種類 | 効果 |
| --- | --- | --- |
| **デカスパナ** | 武器（5回） | 通常攻撃が大振りのスパナ攻撃に変化。ダメージ・吹き飛ばしともに大きいが、発生が遅くガード・回避されやすい |
| **ドラム缶ボム** | 投擲（1回） | 前方に放物線で投げつけ、着弾で爆発。自分も巻き込まれるので足元に落とさないこと |
| **ジャンクブラスター** | 射撃（6回） | 前方へまっすぐ弾を連射。1発の威力は低いが遠距離から削れる |
| **ジャンクバーガー** | 即時 | 拾った瞬間にダメージ%を32回復 |
| **ゴールドギア** | 即時 | 拾った瞬間から6.5秒間、無敵＋移動速度上昇（落下死は防げない） |

- 手に持てるアイテムは1つまで。使い切るか制限時間が過ぎると壊れて消えます
- ストックを失うと持っていたアイテムも失われます
- 必殺技と強攻撃はアイテムを持っていてもそのまま使えます（変化するのは通常攻撃のみ）
- 出現頻度はポーズメニューの「アイテム」から **なし / 少なめ / ふつう / 多め** を
  選択でき、設定は保存されます

アイテムの性能は `src/items/itemData.ts` にデータとして分離してあり、種類の追加や
バランス調整はこのファイルの数値変更だけで行えます。

## ステージ「ジョー・ジャンクション」

廃品置き場と工場を組み合わせた3Dアリーナ。中央の広い戦闘エリアの周囲に
コンテナ・鉄骨・ドラム缶・自動販売機・小型クレーン・パイプ・高低差のある
足場を配置しています。木箱・ドラム缶・壁・看板・自動販売機は攻撃や
吹き飛ばされたキャラクターの衝突で破壊可能で、ドラム缶は強い攻撃を受けると
爆発して周囲を巻き込みます。破片・パーティクル数には上限があります。

## CPU AI

- 3体のCPUはそれぞれ異なる性格パラメータ（攻撃的 / 慎重 / 遠距離重視）を持ち、
  同じ行動を取り続けないようになっています
- プレイヤーだけでなく、近い敵やダメージの高い敵を状況に応じて狙います
- 攻撃の回避・ガード、ステージ端からの回避、落下時の復帰、障害物の回避を行います
- 判断は毎フレームではなく一定間隔で更新し、処理負荷を抑えています
- 難易度 Easy / Normal / Hard をポーズメニューから変更可能（既定はNormal）

## パフォーマンス設定

ポーズメニューから画質 Low / Medium / High を選択できます（スマートフォンでは
自動的に Medium が初期選択されます）。ピクセル比の上限、影の有無、パーティクル数・
破片数の上限、描画距離が画質ごとに調整され、タブが非表示の間は描画・更新を停止します。

## ファイル構成

```
src/
  main.ts                 エントリーポイント
  Game.ts                 全サブシステムを統括するオーケストレーター
  style.css                HUD/モバイル操作UIのスタイル
  config/
    gameConfig.ts          全体バランス・画質プリセット・難易度設定
    controls.ts             PCキーバインド定義
  core/
    GameLoop.ts             固定タイムステップのゲームループ
    InputManager.ts         キーボード/マウス/タッチの入力を統合
    AudioManager.ts          Web Audio APIによる効果音生成・BGM再生
    EffectManager.ts         パーティクル/破片/衝撃波エフェクト（プール管理）
  characters/
    characterData.ts         キャラクター数値データ
    attacks.ts                攻撃（通常/強/必殺技）の数値データ
    abilities.ts               必殺技の特殊挙動の定数
    meshKit.ts                 モデル用のマテリアル/形状/骨格ヘルパー
    characterMeshFactory.ts    キャラクターモデル生成
    Character.ts               キャラクターの状態・メッシュ保持クラス
    CharacterController.ts     移動/攻撃/ガード/回避のステートマシン
  ai/
    AIState.ts                CPU性格パラメータ
    CPUController.ts           CPUの意思決定・操作ロジック
  items/
    itemData.ts                アイテム数値データ（性能・出現重み）
    itemMeshFactory.ts          アイテムモデル生成
    ItemManager.ts              出現・落下・取得・使用・投射物の管理
  combat/
    Hitbox.ts                  当たり判定（前方コーン判定）
    KnockbackSystem.ts          吹き飛ばし計算式
    CombatSystem.ts             攻撃判定解決・ガード・地雷・爆発処理
  stage/
    Stage.ts                   アリーナ生成・地形・当たり判定
    DestructibleObject.ts       破壊可能オブジェクト
  camera/
    CameraController.ts        3人称追従カメラ（衝突回避・ヒットストップ）
  render/
    celShading.ts               トゥーンマテリアル・輪郭線（インク）生成
    CharacterPreview.ts          キャラクター選択画面の3Dプレビュー（専用シーン）
  ui/
    UIManager.ts                HUD（ダメージ%/ストック/タイマー/勝敗画面等）
    CharacterSelect.ts           キャラクター選択画面（カード・性能バー）
    MobileControls.ts            仮想スティック・タッチボタン
  utils/
    math.ts / dispose.ts / objectPool.ts   共通ユーティリティ
```

## デバッグ方法

**F3** キーでデバッグ表示のON/OFFを切り替えられます。表示内容:

- FPS、シーン内オブジェクト数、パーティクル数、設置中の地雷数
- 出現中のアイテム数・投射物数・アイテム出現頻度設定と、各アイテムの座標
- 各キャラクターの座標・速度・ダメージ%・現在の状態
- CPUキャラクターが現在ターゲットにしている相手のID

本番プレイ時は初期状態で非表示です。

## 既知の制限事項（MVPスコープ）

- ステージは1種類、キャラクターは4種類のみ
- 高低差プラットフォームは簡易衝突判定のため、側面からは視覚的にすり抜けて
  見える場合があります（縦方向の乗り降りは正しく機能します）
- ステージ端の自動崖つかまりは未実装（二段ジャンプ・空中回避のみで復帰）
- キャラクター選択はプレイヤー1人分のみ（CPU側の担当キャラクターは固定で、
  同キャラクター同士の対戦もできません）
- 破壊済みオブジェクトは「最初から」でのみ復元されます（試合中の自然復活なし）
- アイテムは同時に1つしか持てず、任意で捨てる操作や、持ったまま投げ渡す・
  相手の落としたアイテムを奪うといった操作はありません
- 効果音は簡易的な生成音のみ

## 今後追加できる機能

- 追加キャラクター、CPU側のキャラクター指定・同キャラクター対戦
- アイテムの種類追加、アイテムを捨てる／奪う操作、種類ごとの出現ON/OFF設定
- ステージ追加、ステージ選択
- オンライン/ローカル対戦（2人以上のプレイヤー操作）
- キャラクターごとの復帰専用アクション、崖つかまりシステム
- リプレイ・戦績記録
- SEバリエーション拡充、BGMの試合展開に応じた切り替え
